{
  "group_blacklist": {
    "description": "群聊黑名单",
    "type": "list",
    "default": [],
    "hint": "填写纯数字群号，例如 123456789；黑名单群不会响应插件"
  },
  "api_base": {
    "description": "API 基址",
    "type": "string",
    "default": "https://newapi.qianqianye.com/v1",
    "hint": "通常保持默认值，不要填写 /chat/completions"
  },
  "api_key": {
    "description": "NewAPI API Key",
    "type": "string",
    "default": "",
    "hint": "在令牌页面生成；仅保存在 AstrBot 插件配置中"
  },
  "model": {
    "description": "图像模型",
    "type": "string",
    "default": "gemini-3.1-flash-image",
    "hint": "默认使用 gemini-3.1-flash-image"
  },
  "temperature": {
    "description": "Temperature",
    "type": "float",
    "default": 0.7,
    "hint": "发送给 chat/completions 的 temperature 参数"
  },
  "timeout": {
    "description": "请求超时秒数",
    "type": "int",
    "default": 180,
    "hint": "图片生成通常需要几十秒"
  },
  "max_retry": {
    "description": "失败重试次数",
    "type": "int",
    "default": 2,
    "hint": "仅对网络错误、429 和 5xx 重试"
  },
  "retry_delay": {
    "description": "重试间隔秒数",
    "type": "int",
    "default": 2,
    "hint": "429 或服务端错误后的等待时间"
  },
  "enable_reply_image": {
    "description": "启用回复图片编辑",
    "type": "bool",
    "default": true,
    "hint": "回复一张图片并使用 /hajimi 或 /kkt 提示词时，将图片作为参考图"
  },
  "enable_at_avatar": {
    "description": "启用 @用户头像",
    "type": "bool",
    "default": false,
    "hint": "开启后收集消息中全部 @ 用户头像作为参考图，可与引用图/当前图片合并（如：引用底图 + @某人 把主角换成他）。需 QQ/aiocqhttp"
  },
  "reply_with_quote": {
    "description": "出图时引用原指令",
    "type": "bool",
    "default": true,
    "hint": "生成图片后引用回复触发 /kkt 或 /hajimi 的那条消息；关闭则只发图"
  },
  "reaction_emoji_enabled": {
    "description": "触发生图时表情回应",
    "type": "bool",
    "default": true,
    "hint": "识别到指令后对原消息点表情（不是文字回复），提示已开始处理。需 QQ/aiocqhttp，仅群聊"
  },
  "reaction_emoji_list": {
    "description": "回应表情ID列表(0~5个)",
    "type": "list",
    "default": [147],
    "hint": "QQ emoji_id,空列表=不回应,最多5个。常用:棒棒糖=147 OK=124 赞=76 爱心=66。完整列表: https://bot.q.qq.com/wiki/develop/api-v2/openapi/emoji/model.html"
  },
  "reaction_emoji_strategy": {
    "description": "回应策略",
    "type": "string",
    "default": "随机",
    "options": ["随机", "顺序循环"],
    "hint": "随机:每次从列表抽1个。顺序循环:按列表依次全部回应,每个间隔0.5s(同一消息多表情)。"
  },
  "cooldown_seconds": {
    "description": "用户冷却秒数(CD)",
    "type": "int",
    "default": 15,
    "hint": "同一用户两次生图的最小间隔，仅对该用户生效；0=关闭。管理员不受 CD 限制"
  },
  "daily_quota": {
    "description": "单日总配额",
    "type": "int",
    "default": 50,
    "hint": "全服每天最多生图次数；0=不限制。超出后普通用户不可用，仅管理员可继续调用"
  },
  "cleanup_delay": {
    "description": "临时文件清理延迟秒数",
    "type": "int",
    "default": 15,
    "hint": "发送图片后等待一段时间再删除本地文件"
  }
}
